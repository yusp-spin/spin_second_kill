package com.spin.kill.server.service;

public interface SbKillService {

    Boolean killItems1(Integer killId, Integer userId);

    Boolean killItems2(Integer killId, Integer userId);

    Boolean killItems3(Integer killId, Integer userId);
}
